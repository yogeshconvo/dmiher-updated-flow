/**
 * SectionMapper is NOT needed.
 * Section keys are registered in each section's index.js and merged in PageView.
 * Adding keys here would duplicate and potentially override them.
 *
 * See: src/sections/Subpages/index.js  ← register new sections here
 *      src/PageView.jsx                ← where all section maps are merged
 */
export {};
